# Blockchain Module Integration Guide

## For Backend Engineer (Zia)

### Overview
This module provides blockchain-based transaction integrity for the payment system. It generates SHA-256 hashes of transactions and stores them on the Ethereum blockchain (Sepolia testnet).

### Installation

1. **Install required packages:**
```bash
pip install web3 requests python-dotenv eth-account