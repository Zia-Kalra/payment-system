# Vite exposes only variables prefixed with VITE_ to the browser.

# Use mock API locally (recommended)
VITE_API_MOCK=true

# Base URL for your backend (only used when VITE_API_MOCK=false)
VITE_API_BASE_URL=http://localhost:8000

